fastapi==0.115.12
uvicorn[standard]==0.34.1
httpx==0.28.1
markdown==3.8
playwright==1.52.0
email-validator==2.2.0
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.24.0
fear-and-greed>=0.4
edgartools>=2.0.0
feedparser>=6.0.0
APScheduler>=3.10.0
# Telegram bot
python-telegram-bot[job-queue]>=21.0
# Bot DB (direct Postgres access)
asyncpg>=0.29.0
# NYSE trading calendar
pandas-market-calendars>=4.0
# Whop forum auto-publisher
whop-sdk>=0.0.38
# X (Twitter) marketing pipeline
tweepy>=4.16.0
anthropic>=0.100.0
# OpenAI-compatible fallback composer (chat completions) for when the
# Anthropic proxy is rate-limited; works with fox / openrouter / etc.
openai>=1.40.0
# Test runner (worker/pytest.ini)
pytest>=9.0
pytest-asyncio>=1.0
